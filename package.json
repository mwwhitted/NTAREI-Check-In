{
  "name": "ntarei-checkin",
  "version": "1.0.0",
  "private": true,
  "description": "Offline attendee check-in PWA for NTAREI meetings (Phase 1)",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "npx --yes serve -l 8080 ."
  },
  "devDependencies": {
    "fake-indexeddb": "^6.0.0"
  }
}
